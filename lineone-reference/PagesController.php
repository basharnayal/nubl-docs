<?php
/**
 * Lineone PagesController - REFERENCE
 * Maps all Lineone demo pages to views.
 * Full implementation: see routes-web-full.php for route → method mapping.
 *
 * View path: pages/xxx or lineone-pages.xxx
 */
namespace App\Http\Controllers;

class PagesController extends Controller
{
    // Elements (14): avatar, alert, badge, breadcrumb, button, button-group, card,
    // divider, mask, progress, skeleton, spinner, tag, tooltip, typography
    // Components (19): accordion, collapse, tab, dropdown, popover, modal, drawer,
    // steps, timeline, pagination, menu-list, treeview, table, table-advanced,
    // table-gridjs, apexchart, carousel, notification, extension-*
    // Forms (20): layout-v1..v5, input-text, input-group, input-mask, checkbox, radio,
    // switch, select, tom-select, textarea, range, datepicker, timepicker, datetimepicker,
    // text-editor, upload, validation
    // Layouts (40+): onboarding, user-card, blog-card, help, price-list, invoice,
    // sign-in/up, error-*, starter-*
    // Apps (11): chat, ai-chat, filemanager, kanban, list, mail, nft-1/2, pos, todo, travel, jobs-board
    // Dashboards (20): crm-analytics, orders, crypto, banking, personal, cms-analytics,
    // influencer, travel, teacher, education, authors, doctor, employees, workspaces,
    // meetings, project-boards, widget-ui, widget-contacts
}
